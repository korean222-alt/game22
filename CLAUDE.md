# 저장소 작업 지침

Claude Code 세션이 이 저장소에서 작업할 때 항상 지켜야 하는 규칙.
게임 자체에 대한 설명은 [README](README.md), 설계 의도는
[docs/GAME_DESIGN.md](docs/GAME_DESIGN.md), 코드 지도와 함정은
[docs/HANDOFF.md](docs/HANDOFF.md) 를 보라.

## 배포 원칙 — 수정하면 무조건 배포한다

**이 저장소의 코드(또는 배포에 영향을 주는 설정)를 수정했다면, 그 작업은
배포까지 끝내야 완료된 것이다.** 수정만 하고 배포를 건너뛰는 것은 허용되지
않는다. 예외 없음 — 사용자가 "배포는 하지 마" 라고 명시적으로 말리지 않는
한, 코드를 바꾼 세션은 반드시 배포까지 마친다.

절차:

1. **머지가 필요한 변경이면 먼저 머지한다.** PR 상태를 확인하고, 충돌이
   있으면 양쪽의 의도를 보존하며 직접 해결한 뒤 머지한다.
2. **base 브랜치(`claude/social-game-story-3d-b3kfse`)에 반영되면 Vercel
   Git 연동이 자동으로 프로덕션 배포를 시작한다** (`vercel.json` 참고,
   프로젝트: `game22` / `social-dev-story-3d`). 별도의 배포 명령을 칠 필요는
   없지만, 그 배포가 **성공**했는지까지 확인해야 한다 — 커밋을 푸시하는
   순간이 아니라 배포가 초록불로 끝나는 순간이 작업의 끝이다.
3. 자동 배포가 없는 상황(원격 저장소가 없거나 Git 연동이 끊긴 경우)이라면
   Vercel MCP 도구(`deploy_to_vercel` 등)로 수동 배포한다.
4. 배포 후에는 실제로 확인한다 — 배포 상태(빌드 로그/체크)를 조회해서
   실패했는지 보고, 실패했다면 원인을 고치고 다시 배포한다. "커밋했으니
   끝났다"고 보고하지 않는다.
5. 사용자에게 결과를 보고할 때는 무엇을 머지/배포했는지와 배포가 실제로
   성공했는지를 함께 말한다.

이 지침은 이 저장소의 모든 브랜치, 모든 세션에 적용된다.
