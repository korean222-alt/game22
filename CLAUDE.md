# 저장소 작업 지침

## 대화 방식 (제일 중요)

- **작업 중에는 아무 말도 하지 않는다.** 중간 진행 보고 금지. 계획 낭독 금지.
  "이제 ~를 하겠습니다", "파일을 확인했습니다" 같은 말은 쓰지 않는다.
- **영어 금지.** 사용자에게 가는 모든 글은 한국어. 영어로 된 중간 로그·요약·
  파일명 나열을 사용자에게 보내지 않는다.
- **끝나고 딱 한 번만 보고한다.** 형식은 아래 그대로:
  - 고친 것 한 줄씩 (무엇이 어떻게 바뀌었나. 파일 경로·함수 이름 나열 금지)
  - 마지막 줄에 배포 주소
  - 그 외 서론·사과·다음 제안·"필요하면 말씀해 주세요" 전부 금지
- 막혀서 물어봐야 할 때만 짧게 한 줄로 묻는다.
- 문서(md)도 같은 원칙: 필요한 것만, 짧게.

## 배포: 수정하면 무조건 배포까지

코드/설정을 고쳤으면 배포 성공 확인까지가 끝. 커밋만 하고 끝냈다고 하지 말 것.

1. 머지 필요하면 먼저 머지 (충돌은 양쪽 의도 보존하며 직접 해결)
2. base 브랜치(`claude/social-game-story-3d-b3kfse`) 반영 → Vercel Git 연동이 자동 배포
3. 연동 안 되면 Vercel MCP `deploy_to_vercel`로 수동 배포
4. 배포 상태 조회해서 실패면 고치고 재배포

## 고치기 전에 확인

- 배치·통로 문제는 눈으로 찍어 볼 것: `python3 -m http.server 8123` 띄우고
  `node tools/probe.mjs` / `tools/raid.mjs` / `tools/flow.mjs`.
- 가구를 늘렸으면 `node tools/kit.mjs "<팩>/Models/OBJ format" assets/furniture/kit.json`.
