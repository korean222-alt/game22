# 인수인계 — 여기까지 왔고, 여기부터 하면 된다

새 세션이 이 파일 하나만 읽고 바로 이어서 작업할 수 있도록 쓴 문서다.
**설계 의도**는 [GAME_DESIGN.md](GAME_DESIGN.md), **플레이 설명**은
[README](../README.md), **원작 조사**는 [RESEARCH.md](RESEARCH.md) 에 있다.
여기에는 그 셋에 안 들어가는 것 — 코드 규약, 함정, 튜닝 손잡이의 위치,
테스트 돌리는 법 — 만 적는다.

마지막 갱신: 2026-09-04 · 브랜치 `claude/boss-monster-game-improvements-u8cpyo`

---

## 0. 30초 요약

카이로소프트 **소셜게임 스토리**를 의존성 없는 WebGL2 3D로 옮긴 리메이크.
**모바일 가로 모드 전용**, Vercel 정적 배포, PWA.

```bash
python3 -m http.server 8123     # 빌드 스텝 없음. ES 모듈이라 정적 서버만 필요
node tools/balance.mjs 12345 10 # 밸런스 시뮬레이션 (브라우저 불필요)
node tools/monster.mjs          # 보스 몬스터 에셋 검증 (브라우저 불필요)
node tools/flow.mjs             # 전체 플로우 브라우저 테스트 (34개 검사)
```

한 바퀴가 전부 돈다: **창업(회사명 + 지원금) → 가구 구입 → 배치 →
채용 → 기획서 → 회의 → 보스 몬스터와의 개발 배틀 → 아이디어 카드 →
완성 → 디버그 → 홍보 → 출시 → 운영 → 팬 → 랭크 → 층 구매 → 다시.**
저장/불러오기, 직원 육성·전직·환생·특성, 연구·계약·유행까지 들어가 있다.

**사무실은 비어 있는 상태로 시작한다.** 창업 멤버도, 책상도 없다.
책상은 사서 놓는 것이고, 앉을 자리가 있어야 사람을 뽑을 수 있다.

---

## 1. 파일 지도

```
src/
  core/     math gl color meshbuilder bake        순수 유틸. 게임도 렌더도 모름
            gltf                                  GLB 리더 + 스켈레톤 솔버 (순수)
  render/   shaders renderer camera               PBR 파이프라인
            skinned                               스킨드 glTF 그리기 (보스 몬스터)
  char/     rig poses                             16본 스켈레톤 (직원)
  world/    palette props office agents           지오메트리 + 직원 상태 기계
            placed                                플레이어가 놓은 가구 → 메시 + 책상
            boss                                  보스 몬스터 상태 기계
  game/     data state staff project economy dialogue   순수 시뮬레이션 (DOM/WebGL 없음)
            furniture monsters tutorial           가구 카탈로그 · 몬스터 정의 · 튜토리얼
  game/     data state staff project economy dialogue events
                                                  순수 시뮬레이션 (DOM/WebGL 없음)
  ui/       hud device style.css                  DOM 오버레이
            firstperson                           1인칭 이동·충돌·상호작용 (규칙은 모름)
  main.js                                         셋을 묶는 View + 부트스트랩
assets/monsters/  cat.glb orc.glb demon.glb       유일한 외부 에셋 (Quaternius CC0)
tools/      balance sloppy flow meeting probe     테스트 하네스 (아래 5장)
            monster gltf2glb shots
```

**경계가 이 프로젝트의 전부다.** `game/` 이 DOM 을 모르기 때문에 밸런스를
브라우저 없이 Node 에서 돌릴 수 있고, 그게 이 게임을 튜닝 가능하게 만든다.
**`game/` 안에 `document`, `window`, `canvas` 를 절대 들이지 말 것.**
(`localStorage` 는 `state.js` 의 save/load 에만 있고 try/catch 로 감싸져 있다.)

### 어디를 고쳐야 하나

| 하고 싶은 것 | 고칠 곳 |
|---|---|
| 밸런스 숫자 | **`game/data.js` 하나뿐** — 장르/플랫폼/직업/랭크/아이템/특성/연구/홍보/계약/층값 |
| 품질·평론 곡선 | `game/project.js` 상단 `QCAP QSCALE CBASE CSPAN CSCALE` |
| 유저 수 · 매출 · 팬 | `game/economy.js` `releaseGame()` |
| 새 시스템 | `game/state.js` 에 액션 추가 → `ui/hud.js` 에 패널 추가 |
| 건물 구조 · 방 | `world/office.js` (배치) + `world/props.js` (부품) |
| 살 수 있는 가구 | `game/furniture.js` (값·효과) + `world/placed.js` (`DRAW` 표) |
| 배치 가능 구역 | `world/office.js` 의 `PLACE_ZONES` |
| 보스 몬스터 | `game/monsters.js` (정의·등급) + `world/boss.js` (연출) |
| 튜토리얼 문구·순서 | `game/tutorial.js` |
| 창업 지원금 · 긴급 지원금 | `game/data.js` 의 `STARTUP_GRANT` · `rescueAmount()` |
| 직원 걷기/회의 행동 | `world/agents.js` |
| 회의 대사 | `game/dialogue.js` |
| 주간 이벤트 · 세일즈 태스크 | `game/events.js` (데이터+효과), `game/state.js` 의 `rollWeeklyEvent`/`checkTasks` |
| 1인칭 이동·상호작용 | `ui/firstperson.js` (조작), `main.js` 의 `wireFirstPerson()` (게임과 연결) |
| 카메라·연출 | `main.js` 의 `View` 클래스 |

---

## 2. 반드시 알아야 할 규약

### 2.1 좌표와 방향 — 여기서 버그가 네 번 났다

- **−Z 가 북쪽.** rig 가 yaw `y` 면 바라보는 방향은 `(sin y, cos y)`.
- **의자 회전은 `yaw − PI/2`.** 좌석은 `yaw` 하나만 들고 다니고 가구
  회전은 거기서 유도한다. 두 숫자를 따로 두면 사람이 뒤돌아 앉는다.
- **`props.js` 에서 무언가를 "옆으로" 밀 때는 `(sin ry, cos ry)` 를 쓴다.**
  `(cos, sin)` 을 쓰면 90° 틀어진다. 실제로 이 실수로:
  - 책상 가림판이 앞이 아니라 **옆면**에 서서 5유닛 벽이 됐고,
  - 문틀 기둥이 벽과 나란하지 않고 **자기 문을 가로막았고**,
  - 계단이 엘리베이터 코어에서 **옆으로 13유닛 뻗어나갔다**.

  새 프롭을 추가하면 `tools/flow.mjs` 가 아니라 **경로 탐색 테스트**
  (`tools/meeting.mjs` 의 첫 검사)로 확인할 것. 벽이 생기면 즉시 잡힌다.

### 2.2 NavGrid 는 area 를 반드시 받는다

`core/bake.js` 의 `new NavGrid(mesh, y0, y1, cell, dilate, area)` —
**`area` 를 생략하면 안 된다.** 메시에는 광장과 스카이라인까지 들어 있어서,
격자가 거기에 맞춰 자기 크기를 잡고 셀 예산(900×900)에 맞추려고 조용히
해상도를 낮춘다. 그러면 `path()` 가 `no route` 를 뱉는다.
지금은 예산 초과 시 **throw** 하도록 해놨으니 조용히 망가지지는 않는다.

층마다 격자가 따로다. 하나로 합치면 모든 층의 가구가 한 장으로 융합돼
전부 막힌다 (`main.js` `build()` 참조).

### 2.3 돌하우스 컷

두 종류가 동시에 돈다.

1. **벽 디더 컷** — 카메라와 시점 사이의 벽이 4×4 오더드 디더로 녹는다.
2. **층 컷** — 보고 있는 층 위쪽이 통째로 잘린다.
   `floorY = view.floor * STOREY + BUILDING.wallH + 0.1`, 램프 0.7.

플래그는 `world/office.js` 의 `buildSite()` 안에 세 종류가 있다.

| 플래그 | 뜻 | 쓰는 곳 |
|---|---|---|
| `0` | 두 컷 모두 적용 | 실내 벽·가구 |
| `3` | 벽 컷만 면제 | 외부 기둥, 파라펫 — 얇아서 디더로 녹여봐야 얼룩만 생긴다 |
| `SITE = 4` | **두 컷 모두 면제** | 지면·도로·주변 빌딩 |

**지붕을 SITE 로 하면 안 된다.** 파라펫은 `3` 이라서 아래층을 보면 같이
사라진다. 예전에 지붕을 통짜 슬래브로 덮었더니 컷 임계선 위에 걸려
화면 전체가 흰 디더가 됐다. 지금은 슬래브가 아니라 **링**이다.
새 야외 프롭을 추가하면 이 표를 보고 플래그를 고를 것.

### 2.4 회의 연출의 프레임 독립성

`main.js` 의 프레임 루프는 `dt` 를 **0.1초로 클램프**한다. 저사양 기기에서
2fps 가 나오면 걷기가 1/20 속도가 되고 회의가 영원히 안 끝난다.
그래서 `playMeeting()` 은 **4.5초 데드라인**을 두고 `crew.forceSeat()` 으로
남은 사람을 자리에 순간이동시킨다. 회의 로직을 손대더라도 이 안전장치는
남겨둘 것 — 없으면 느린 폰에서 게임이 멈춘 것처럼 보인다.

### 2.5 층을 사면 월드를 다시 짓는다

`View.setFloorCount(n)` → `build()` 전체 재실행 (AO 굽기 + 층별 NavGrid).
폰에서 몇 초 걸리므로 `body.busy` + `#busy` 배너가 뜬다.
**`bootStep()` 은 null-safe 여야 한다** — 부트 오버레이는 이미 제거된 뒤다.
(이걸 놓쳐서 "층을 사면 돈만 나가고 아무 일도 안 일어나는" 버그가 있었다.)

---

## 3. 밸런스 — 지금 숫자와 그 이유

### 3.1 도달 곡선 (10년 × 5시드, `tools/balance.mjs`)

| | 랭크 | 출시작 | 평론가 | 명예의 전당 |
|---|:-:|:-:|:-:|:-:|
| 1년차 | 2~3 | 6 | 11~15 | 0 |
| 2년차 | 3~7 | 14~21 | 13~24 | 0 |
| 3년차 | 8~10 | 25~31 | 19~25 | 0 |
| 5년차 | 11~13 | 38~56 | 28~35 | **첫 등재 (0~14)** |
| 7년차 | 13~15 | 65~83 | 30~35 | 14~35 |
| 10년차 | 15~16 | 100~124 | 30~36 | 42~73 |

첫 명예의 전당(32점)이 4~5년차에 오는 건 **핵심 6명이 두 번째 전직을
마치는 시점**이라서다. 우연이 아니라 구조이므로, 전직 조건이나
`maxLevel = 30 + tier*30` 을 건드리면 이 시점이 통째로 움직인다.

### 3.2 두 개의 포화 곡선 (`game/project.js` 상단)

```js
const QCAP = 999, QSCALE = 560;   // 품질 = 999(1 - e^(-x/560))
const CBASE = 2.2, CSPAN = 7.8, CSCALE = 330;  // 평론가 1인 = 2.2 + 7.8(1 - e^(-v/330))
```

`x` = **1인·1턴당 기여**. 창업 ~10 에서 성숙기 ~450 까지 간다.
`project.rawPerSlot` 에 그대로 담아두므로 시뮬레이션에서 바로 볼 수 있다.

곡선이 반드시 포화형이어야 하는 이유: 능력치가 레벨×전직×의욕×궁합×
개발방식으로 **곱해지며** 네 자릿수를 넘어간다. 선형으로 두면 2년차에
차트를 벗어난다.

### 3.3 처리량(연간 출시작)을 결정하는 것

```
연간 출시작 ≈ 48주 × 주간 스태미나 ÷ (게임당 턴수 × 턴당 스태미나)
```

- **주간 스태미나**: `data.js` `rankInfo().staminaMax = min(38, 8 + rank)`
- **게임당 턴수**: `project.js` `hp = 8600 × 장르 × 플랫폼 × 시리즈 × 등급 × (1 + (랭크-1)×0.11)`

**랭크 계수가 핵심이다.** 회사의 턴당 데미지는 복리로 크는데 HP 는
플랫폼 사다리만큼만 커서, 이게 없으면 성숙한 스튜디오가 매주 한 작품씩
찍어낸다 (실측: 10년차 300작 → 120작). 처리량이 이상해지면 **여기부터** 볼 것.

### 3.4 팬 → 랭크

```js
RANK_UP_FANS = 900 × 2.2^(rank-1)     // data.js
fansGained  = users × 0.11 × (명예의전당 1.5) × 홍보 × 스타개발자   // economy.js
users       = pull × 26 × 플랫폼 × 수익모델 × ... × fanBoost(상한 9)
```

`fanBoost` 상한이 없으면 유저→팬→유저로 자기를 먹여 2년차에 폭주한다.

### 3.5 성장의 느린 길 (절대 막히면 안 되는 것)

- 아이템 = 빠른 길. 돈 + 스태미나(`trainStamina = 1 + level/22`)를 쓴다.
- **출시 = 느린 길.** `state._completeProject()` 에서 팀원이
  `xp = 20 + hpMax/300 + 평론×2` 를 받는다. `expToNext = 18 × level^1.15`.

이게 없으면 돈을 안 쓰는 플레이어가 **레벨 3에 영구히 묶인다**.
`tools/sloppy.mjs` 가 정확히 그 상태를 검사한다.

### 3.6 창업 지원금과 긴급 지원금

**창업 지원금 `STARTUP_GRANT = 180,000`** 이 시작 자본의 전부다. 별도의
시작 잔고는 없다. 이 숫자는 반올림해서 정한 게 아니라 초반 지출에서
역산했다 — 기본 책상 3개(27,000) + 신입 3명(약 20,000) + 피처폰
프로젝트 한 편(25,000) + 두어 달 인건비를 내고도 사무실을 사람 사는
곳으로 만들 여유가 남되, 첫 결정이 공짜가 되지는 않을 만큼.

**긴급 지원금**은 예전의 "조용히 40,000 채워주기"를 대체한다.

```js
amount = max(30,000, 120,000 × 0.72^n, need)      // data.js rescueAmount
need   = 가장 싼 플랫폼 개발비 × 1.8 + 주간비용 × 4   // state._rescue
morale = −(1 + min(3, n))
```

`need` 항이 핵심이다. 이게 없으면 지원금이 게임 한 편 값 아래로
내려가고, 회사는 **살아는 있는데 아무것도 못 하는** 상태가 된다.
파산과 똑같은 막다른 길에 절차만 하나 더 붙은 셈이다.
`tools/sloppy.mjs` 가 실제로 8년차에 거기 갇혀서 이 항이 생겼다.

정상적인 탈출구는 여전히 **계약 일감**(랭크 1에 14,000~42,000):
스태미나만 쓰고 의욕은 깎이지 않으니 언제나 지원금보다 싸다.

`tools/sloppy.mjs` 로 8년 돌리면 파산하지 않고 38편을 내놓지만
유저가 바닥(400명)에 붙어 랭크 3에 머문다.
**"죽지는 않되 아무것도 나아지지 않는" 상태가 설계된 하한선이다.**

### 3.7 버그 개수와 디버그

```js
scale = 8 + 12 × log2(1 + hpMax/4500);   bugs = scale × (1.5 − min(1.2, 조작성/70))
fixed = max(팀 기여도, 남은 버그 × 0.3) × (0.7~1.3)          // project.debug
```

HP 에 **선형이면 안 된다.** 예전에 중반 프로젝트에 163개가 붙었고,
디버그 스무 번은 선택이 아니라 노가다였다.

`debug()` 가 **남은 개수의 비율**을 잡는 것도 같은 이유다. 창업 멤버가
사라지면서 첫 게임은 레벨 1 신입들이 만들게 됐고, 조작성이 바닥이라
버그가 50개씩 나온다. 그 자체는 옳다 — 조작성이 스탯인 이유가 그거다.
하지만 한 번에 6개씩 지우면 여덟 번을 눌러야 한다. 비율 항이 붙으면
개수와 상관없이 대여섯 번 안에 끝난다. `tools/flow.mjs` 가 개수가 아니라
**누른 횟수**를 검사하는 것이 이 규칙의 표현이다.

### 3.8 책상이 곧 정원

랭크는 `staffCap` 을 올리지만, 실제 채용 한도는 **빈 책상 수**다
(`state.freeDesks()`). 사무실과 인사를 잇는 유일한 규칙이고,
게임이 채용 화면이 아니라 가구점에서 시작하는 이유다.

배치 구역에 들어가는 기본 책상 수: 1층 10개, 2~5층 18~20개.
랭크 상한(24명)보다 넉넉하다 — 이 수치는
`world/office.js` 의 `PLACE_ZONES` 를 건드릴 때마다 다시 재볼 것.

### 3.9 쾌적도

```js
per = Σ가구 comfort / 직원 수;   t = 1 − exp(−per/7)
moodGain  = t × 0.55      // 주간, 유휴 직원 의욕이 오를 확률
planBonus = 1 + t × 0.18  // 기획력 배율
```

**1인당**으로 재는 게 요점이다. 두 명 사무실에 소파 열 개는 좋은
사무실이 아니라 창고다. 포화 곡선이 없으면 가장 싼 전략은 모든 층을
₩2,500 화분으로 도배하는 것이 된다.

---

## 4. 알려진 함정 (다음 사람이 또 밟을 것들)

| 함정 | 실제로 일어난 일 |
|---|---|
| `bootStep()` 이 부트 오버레이 제거 후 throw | 층을 사면 돈만 나가고 월드가 조용히 그대로. **now null-safe** |
| NavGrid 에 `area` 미지정 | 격자가 스카이라인까지 덮고 해상도가 낮아져 `no route` |
| `props.js` 에서 `(cos, sin)` 사용 | 가구가 90° 틀어져 통로를 막는 벽이 됨 |
| 옥상을 통짜 슬래브로 덮음 | 층 컷 임계선 위에 걸려 화면 전체가 흰 디더. 지금은 파라펫 **링** |
| 모달 클래스는 `.show` 다 (`.hide` 아님) | 테스트에서 모달 감지가 반대로 돌아 배틀 루프가 무한정 |
| `confirm()` 은 `#mOk` 가 아니라 `#mOpts .choice` | "예" 를 클릭해야 한다. `mOk` 는 `display:none` |
| 완성작 패널은 **개발 탭**에 있다 (운영 아님) | `panelDev()` 가 `g.finished` 면 `panelFinished()` 로 분기 |
| 테스트에서 `.item.click` 을 먼저 찾으면 | 직원 카드가 자식 버튼의 텍스트를 포함해서, 버튼 대신 카드가 눌려 패널이 접힌다. **버튼 먼저 검색** |
| 900px 이상 뷰포트에서 패널이 접혀 시작 | 테스트는 `body.classList.remove('panel-hidden')` 로 명시적으로 열 것 |
| SwiftShader 에서 1~2fps | 소프트웨어 래스터라이저 + DPR 2 + 5단 블룸. **실기기 문제 아님.** 테스트는 딜레이 대신 폴링할 것 |
| 서버가 프로세스 그룹 종료로 죽음 | `setsid` 로 띄울 것 |
| 보스가 자기 셰이더를 쓰고 나면 유리가 안 그려짐 | 유니폼은 **프로그램별** 상태다. `renderer.render()` 가 유리 패스 직전에 `pScene` 을 다시 바인딩한다. 스킨드 패스도 `end()` 로 되돌린다 |
| 조인트 43개를 유니폼 배열에 | WebGL2 최소 보장이 vertex uniform vector 256개인데 mat4 43개가 그걸 통째로 먹는다. 최소치를 그대로 보고하는 폰 드라이버가 실제로 있다. **RGBA32F 텍스처 + texelFetch** |
| 새 직원이 화면에 안 나타남 | `hire()` 는 `staff` 이벤트만 쏜다. `ui/hud.js` 가 그걸 `view.syncAgents()` 로 연결한다. 사무실이 비어서 시작하게 되면서 드러난 오래된 버그 |
| 배치 검사에 가구가 포함된 격자를 씀 | 옮기려는 가구 자신과 충돌한다. 격자는 **두 벌** — `navBase`(건물만, 배치 검사용)와 `navs`(건물+가구, 길찾기용) |
| 배치 구역 없이 벽 충돌만 검사 | 출입구와 승강기 로비는 빈 공간이라 통과한다. `PLACE_ZONES` 가 **먼저** 걸러야 한다 |
| 랭크 1 `staffCap` 이 창업 멤버 수와 같음 | 첫날부터 채용이 전부 "정원 초과". 지금은 7명(창업 5명)으로 여유를 둔다 |
| `View.syncAgents()` 를 채용/퇴사에서 부르지 않음 | 퇴사자가 자리에 계속 앉아 있고 신입은 3D 에 안 나타난다. `state` 가 `hired`/`fired`/`staff` 를 emit 하고 `main.js` 가 받는다 |
| 1-999 품질을 `/4` 해서 막대 폭으로 | 데뷔작 9점이 2% 막대 → "그래프가 안 올라온다". `hud.js` 의 `qBar()` 포화 곡선을 쓸 것 |
| `monitor()` 를 데스크 깊이 축으로 만듦 | 화면이 옆을 보고, 게다가 와인딩이 뒤집혀 뒷면이 앞이었다. 지금은 좌석 방향으로 향한다 (`props.js` 주석의 FACING 절) |
| 이동 슬라이드에서 축 성분이 1e-16 | 정북으로 걸으면 dx≈0 인데 "x 로 이동했다" 처리되어 제자리. `firstperson.js` 의 EPS |
| 선택지 이벤트를 답하지 않고 `nextWeek()` | 주가 넘어가지 않는다 (`{blocked:true}` 반환). 하네스는 `answerEvent(0)` 을 부를 것 |

---

## 5. 테스트 하네스 (`tools/`)

### 브라우저 불필요 — 밸런스 · 에셋

```bash
node tools/balance.mjs [시드] [연차]   # 잘 두는 AI, 기본 12345 5
node tools/sloppy.mjs  [시드]          # 최악의 플레이, 파산 방지선 검사
node tools/monster.mjs                 # 보스 에셋: 스킨·클립·조인트·NaN 검사
```

`balance.mjs` 는 연차별 표(랭크·자금·팬·출시작·평론가·평균품질·레벨·
명예의 전당·직급분포)와 초기 20작의 `x` / 품질 / 평론 추이를 찍는다.
**밸런스를 건드렸으면 최소 3개 시드로 돌려볼 것.** 한 시드는 거짓말을 한다.

두 시뮬레이션 모두 이제 **가구를 사서 놓는다.** 사무실이 비어서 시작하니
책상 없이는 채용이 안 되고, 채용이 안 되면 아무것도 측정할 수 없다.
`clearOfWalls` 는 `() => true` 로 스텁이다 — 그 검사에는 WebGL 이 만든
충돌 격자가 필요하고, 배치 구역 검사만으로도 헤드리스에서는 충분하다.

`monster.mjs` 는 `core/gltf.js` 의 스켈레톤 솔버가 순수하기 때문에
브라우저 없이 돈다. 클립을 한 프레임씩 밟으면서 조인트 행렬이 유한한지,
실제로 움직이는지, 정점이 참조하는 조인트 번호가 범위 안인지 본다.
**`.glb` 를 다시 만들면 이걸 먼저 돌릴 것.**

```bash
node tools/gltf2glb.mjs in.gltf out.glb Idle,Walk,Punch,HitReact,Death
```

`.gltf`(base64 내장) → `.glb` 재포장 + 안 쓰는 클립 제거. 오크 기준
1.25MB → 425KB. 몬스터 팩은 클립 14개를 싣고 오는데 게임은 5개만 쓴다.

### 브라우저 필요

```bash
(setsid python3 -m http.server 8123 &)      # setsid 중요
node tools/probe.mjs      # 부팅만 (에러/단계별 타이밍/월드 통계)
node tools/meeting.mjs    # 창업·가구·채용 → 경로탐색 → 회의 → 착석 → 복귀
node tools/flow.mjs       # 전체 34개 검사. 지금 34/34 통과
node tools/shots.mjs      # 눈으로 볼 것들의 스크린샷 (CI 아님)
node tools/meeting.mjs    # 경로탐색 → 회의 시작 → 착석 → 말풍선 → 복귀
node tools/flow.mjs       # 핵심 루프 19개 검사. 지금 19/19 통과
node tools/features.mjs  # 채용·퇴사 연출, 기획서 폐기, 디버그 점수, 품질 막대,
                         # 미발견 조합, 주간 이벤트, 세일즈 태스크, 1인칭. 19/19
```

환경변수: `PLAYWRIGHT=` (기본 `/opt/node22/.../playwright/index.mjs`),
`PORT=` 또는 `BASE=`, `OUT=` (스크린샷 저장 위치).

`flow.mjs` 는 실제 DOM 컨트롤을 눌러서 렌더 경로가 깨지면 소리를 낸다.
게임 객체는 "한 시간 플레이해야 도달하는 상황"(랭크·자금) 을 만들 때만 쓴다.
SwiftShader 가 느리므로 **고정 딜레이 대신 상태를 폴링**하도록 짜여 있다.

`flow.mjs` 마지막에 `flow-final.png` 가 떨어진다. 렌더링이 눈으로 멀쩡한지
확인할 때 쓸 것. `features.mjs` 는 단계마다 스크린샷을 남긴다 (`01-a2hs.png`
… `06-back.png`).

`tools/` 하네스는 `tools/` 이름을 README 의 구조 목록에도 넣어둔다.

`shots.mjs` 는 **숫자로 검사할 수 없는 것**을 위한 것이다. 스킨드 몬스터는
조인트가 전부 유한하고 올바른 클립이 돌고 GPU 에 메시가 올라가 있어도
안팎이 뒤집혔거나, 열 배 크거나, 남의 팔꿈치로 텍스처링돼 있을 수 있다.
창업 화면 · 빈 사무실 · 배치 모드 · 가구 놓은 사무실 · 몬스터 3종 ·
전투 UI · 사무실 패널을 PNG 로 떨군다.

---

## 6. 배포

Vercel 정적 배포. 빌드 스텝이 없다.

```bash
npx vercel --prod
```

- `vercel.json` 은 캐시 헤더만 정한다. `sw.js`/`src/` 는 매번 재검증.
- **`sw.js` 에 파일 두 개를 챙겨야 한다**: `ASSETS` 목록에 새 모듈 추가 +
  `CACHE` 버전 올리기 (현재 `sds3d-v3`). 안 하면 업데이트가 안 퍼진다.
- `assets/monsters/*.glb` 는 **일부러 프리캐시하지 않는다.** 셋이 합쳐
  1MB 에 가깝고 프로젝트당 한 종만 나오므로, 런타임 fetch 핸들러가
  처음 쓸 때 캐시한다. 첫 실행이 가벼워지고 두 번째부터는 즉시 뜬다.
- 서비스 워커는 HTTPS 에서만 등록되므로 로컬에서는 오프라인 캐시가 꺼진다.

목록이 최신인지 확인:

```bash
ls src/*/*.js src/*.js | sed 's|^|"./|;s|$|",|' | sort   # sw.js 의 목록과 비교
```

---

## 7. 다음에 할 만한 것

우선순위 순. 전부 "지금 구조에서 자연스럽게 이어지는" 것들이다.

1. **가구 확장** — 카탈로그(`game/furniture.js`)에 줄 하나, `DRAW`
   표(`world/placed.js`)에 줄 하나면 새 가구가 늘어난다. `props.js` 에
   부품이 이미 50종 있으므로 대부분은 배선만 하면 된다. 외부 3D 모델을
   가구로 쓰고 싶으면 `render/skinned.js` 가 이미 텍스처드 glTF 를
   그릴 수 있다 — 스킨 없는 모델은 `uSkinned=0` 으로 가면 된다.
2. **직원이 가구를 쓰게 하기** — 지금 쾌적도는 숫자로만 작동한다.
   `world/agents.js` 의 ambient 스폿을 플레이어가 놓은 소파·커피머신에서
   생성하면, 좋은 사무실이 눈에 보이게 된다. `placed.js` 가 이미 위치를
   알고 있으므로 `out.spots` 를 하나 더 내보내면 끝이다.
3. **보스가 실제로 반격** — 지금 반격은 순수 연출이다. 턴을 되돌리거나
   버그를 얹는 식으로 수치를 붙이려면 `game/project.js` 의 `battleTurn`
   안에서 해야 한다 (`world/boss.js` 는 규칙을 몰라야 한다).
4. **랜덤 이벤트** — 주간 클록(`state.nextWeek()`)에 훅. 서버 다운,
   유명인 트윗, 경쟁사 출시, 직원 번아웃. 유행 시스템과 같은 자리에 붙는다.
5. **자체 플랫폼 개발** — 랭크 20 해금 플랫폼은 있는데 "개발" 과정이 없다.
1. **시설 배치** — 층마다 시설 슬롯(서버랙/사운드부스/휴게실/회의실 확장).
   `world/office.js` 는 이미 방을 rect 로 잡고 있으니 슬롯을 데이터로
   빼고 `game/state.js` 에 구매 액션을 붙이면 된다. 원작의 큰 축인데
   지금은 층 역할(`FLOOR_PLANS[].role`)만 있다.
2. ~~**랜덤 이벤트**~~ — 구현됨. `game/events.js` 의 `EVENTS` 12종,
   주당 21% 확률로 `nextWeek()` 끝에서 뽑는다. 선택지가 있는 사건은
   `pendingEvent` 로 남아 답하기 전까지 주를 막는다. 새 사건은 배열에
   한 항목만 추가하면 되고, `when`/`pick`/`vars`/`apply`(또는 `choices`)
   네 개가 계약의 전부다.
3. **자체 플랫폼 개발** — 랭크 20 해금 플랫폼은 있는데 "개발" 과정이 없다.
   `data.js` PLATFORMS 의 `own` 을 목표로 하는 별도 트랙.
6. **속편/시리즈 UI 다듬기** — 로직은 있다(`seriesOf`, `seriesN`).
   명예의 전당 목록에서 바로 속편을 만들 수 있게.
7. **사운드** — 지금 완전 무음. 클릭/크리티컬/출시 정도만 있어도 크다.
   WebAudio 로 절차적 생성하면 에셋 파일 없이 갈 수 있다.
8. **후반 성장** — 지금 `x` 가 ~450 에서 정체한다. 환생(재능 ×1.12)이
   유일한 돌파구인데 UI 유도가 약하다. 10년 넘게 가는 세이브를 원하면
   여기가 병목이다.
7. **1인칭 확장** — 지금은 걷기 + 격려 + 비품 한 마디까지다. 자연스러운
   다음 수는 책상 앞에서 그 사람의 상세 패널 열기, 회의실에서 회의 소집,
   그리고 사장 아바타를 실제로 그려 다른 직원이 반응하게 하는 것.
8. **보조자 / GameDex** — 분석 문서(B-11, B-12)의 요일별 보조자와 판매
   부스트 이벤트. 세일즈 태스크와 같은 자리(`game/events.js`)에 붙는다.

### 손대지 말아야 할 것

- **`game/` 에 DOM 을 들이는 것.** 밸런스 시뮬레이션이 죽는다.
- **건물 표면에 텍스처 파일 추가.** 사무실의 모든 표면은 프로시저럴
  머티리얼 16종이고, 이게 이 프로젝트의 기술적 정체성이다.
  보스 몬스터는 의도적인 예외다 — 외부 캐릭터 팩이고, 자기 아틀라스를
  자기 셰이더(`FS_SKIN`)로 그리며 프로시저럴 파이프라인에 손대지 않는다.
  가구를 몬스터처럼 외부 모델로 바꾸고 싶다면 그 경로를 쓸 것.
- **의존성 추가.** `npm install` 없이 도는 것이 요구사항이다.

---

## 8. 커밋 이력

```
eb10944  보스 몬스터 · 빈 사무실에서 시작 · 가구 배치 · 그래프 수정
fbdf465  인수인계 문서 + 테스트 하네스를 저장소로
(this)   채용 · 퇴사 연출 · 1인칭 시점 · 주간 이벤트 · 조합 발견 · 모니터/그래프 수정
eab23bc  회의실 회의 · 사무실 확장 · 분석 문서의 시스템 · 밸런스 재조정
3c026c2  모바일 가로 모드 + PWA
bface46  소셜게임 스토리 3D: WebGL2 PBR 리메이크 뼈대
```

앞의 네 개는 `claude/social-game-story-3d-b3kfse`,
마지막은 `claude/boss-monster-game-improvements-u8cpyo` 에 있다.

## 9. 에셋

`assets/monsters/*.glb` 는 **Quaternius** 의 CC0 몬스터 팩에서 왔다
(원본 아틀라스 이름 `Atlas_Monsters`, Blender glTF 익스포터 v1.7.33).
클립 14개 중 게임이 쓰는 5개만 남기고 `tools/gltf2glb.mjs` 로 재포장했다.

**외부 3D 모델을 더 받아 넣는 방법 — 어디서 받고, 어떤 규격이어야 하고,
어떻게 배선하는지는 [ASSETS.md](ASSETS.md) 에 따로 적었다.**
앞의 세 커밋은 브랜치 `claude/social-game-story-3d-b3kfse`,
마지막은 `claude/game-planning-system-auj1dp` 에 있다.
